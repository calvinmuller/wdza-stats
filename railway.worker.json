{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run db:migrate --workspace=@wdza-stats/db && npm run db:seed --workspace=@wdza-stats/db"
  },
  "deploy": {
    "startCommand": "npm run start --workspace=@wdza-stats/worker",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
